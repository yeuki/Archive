# Local release archive

Each version receives its own folder and immutable APK:

```text
vX.Y.Z/Archive-vX.Y.Z.apk
```

APKs and checksum files are intentionally excluded from Git. GitHub tracks releases through tags, release notes, and release assets; the binary archive also remains available locally and in Google Drive.
